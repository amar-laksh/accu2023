{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug build",
            "type": "shell",
            "command": "cd build && rm -rf && cmake -GNinja .. && ninja"
        },
        {
            "label": "debug run",
            "type": "shell",
            "command": "cd build && rm -rf && cmake -GNinja .. && ninja -j18 && ./src/main"
        }
    ]
}
